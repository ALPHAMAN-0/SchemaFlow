# CLAUDE.md

- No manifest (package.json / pyproject.toml / go.mod / Cargo.toml) exists — no build, test, or lint commands to record.
- Repo currently contains only README.md; there is no code, so no package-manager or generated-directory rules apply yet.
- File worth reading first: /README.md (only content in the repo).

Architecture: see ARCHITECTURE.md — read before structural changes
